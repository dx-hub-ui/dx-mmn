// src/app/auth/callback/page.tsx  (Client fallback; optional but useful)
// If you prefer client-side handling, keep this page; Route Handler above already completes session.
"use client";
export default function CallbackPage() {
  return null; // Route Handler handles the work and redirects
}
